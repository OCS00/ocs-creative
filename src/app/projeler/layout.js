export const metadata = {
  title: "Portfolyo & Başarı Hikayeleri | OCS Creative",
  description: "Sektör liderleri için geliştirdiğimiz seçkin dijital projeler ve başarı hikayeleri.",
};

export default function Layout({ children }) {
  return children;
}